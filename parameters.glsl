// Light and material parameters
const float kd = 1.0;      // diffuse strength
const float ka = 1.0;      // ambient strength
const float id = 0.6;      // diffuse light intensity
const float ia = 0.2;      // ambient light intensity
const vec3 lightDir = normalize(vec3(1,1,0)); // light direction
